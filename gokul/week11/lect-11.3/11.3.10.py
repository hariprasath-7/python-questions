# What is zip in Python?
# The zip() function in Python is a built-in function that takes multiple iterables (like lists, tuples, etc.) as arguments and returns an iterator of tuples.
#  Each tuple contains elements from the input iterables that are at the same position. The zip() function is often used to combine two or more iterables into a single iterable of tuples.
