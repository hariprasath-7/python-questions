# 1. let x store the value of length of the string, what will be the output?, replace LINE-2 with correct code block and predict the output
#     ```
# a="gokull"
# x=line -2 
# print(a[x])

# op
#  x = len(a)
#  and print(a[x]) will not work cause we load the number of characters in x and when we print we use indexing the number of cha = 5 when indec=xing =0-4 so it will not work 