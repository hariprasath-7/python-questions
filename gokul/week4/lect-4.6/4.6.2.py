#
import random
l=random.sample(list(range(10000)),1000)
print(l)

# try guessing the output, and write the same code again
# same code but differnet input
import random
l = random.sample(list(range(100)),10)
print(l)